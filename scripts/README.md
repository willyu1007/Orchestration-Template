# scripts 目录说明

占位：概述结构与使用方式。

- 命名方式：
  - `<功能>_<动作>.<语言后缀>`

- 脚本文件内容：
  - 在文件开头使用简洁声明介绍脚本功能，采用固定格式并用 JSON Schema 约束参数；Schema 文件请放在 `schema/` 目录。
  - 在功能声明之后描述调用协议；暂时无需完整 JSON-RPC，可先约定稳定的本地进程协议（请求、响应、错误）。
